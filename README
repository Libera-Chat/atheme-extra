These are libera.chat-specific modules for atheme-services.

To compile, first compile and install atheme-services. Then copy
Makefile.config.example to Makefile.config and edit it, then make and make
install.
